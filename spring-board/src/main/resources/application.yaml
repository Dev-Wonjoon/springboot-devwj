spring:
  application:
    name: spring-board

  devtools:
    livereload:
      enabled: true

  datasource:
    url: ${DB_URL}
    username: ${DB_USER}
    password: ${DB_PASSWORD}


logging:
  level:
    net.springboard: DEBUG